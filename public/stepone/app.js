const API_BASE=window.location.origin;
const DEADLINE=Date.parse('2026-09-26T23:59:59+05:30');
const CLOSED_MESSAGE='The Step 1 submission deadline has passed. Please contact the course coordinator.';
const form=document.querySelector('#submission-form'),admissionInput=document.querySelector('#admission-number'),repositoryInput=document.querySelector('#repository-url'),lookupButton=document.querySelector('#lookup-button'),submitButton=document.querySelector('#submit-button'),detailPanel=document.querySelector('#team-details'),feedback=document.querySelector('#feedback'),deadline=document.querySelector('#deadline');
let verifiedTeam=null;
const isClosed=()=>Date.now()>DEADLINE;
function showFeedback(text,kind='error'){feedback.textContent=text;feedback.className=`feedback ${kind}`;feedback.hidden=false}
function clearFeedback(){feedback.textContent='';feedback.hidden=true}
function clearTeam(){verifiedTeam=null;detailPanel.hidden=true;repositoryInput.disabled=true;submitButton.disabled=true}
function setBusy(button,busy,label){if(!button.dataset.label)button.dataset.label=button.textContent;button.disabled=busy||isClosed();button.textContent=busy?label:button.dataset.label}
function showTeam(team){verifiedTeam=team;document.querySelector('#leader-name').textContent=team.leaderName;document.querySelector('#team-name').textContent=team.teamName;document.querySelector('#project-name').textContent=`${team.projectId} · ${team.projectTitle}`;detailPanel.hidden=false;repositoryInput.disabled=false;submitButton.disabled=isClosed()}
function applyDeadlineState(){if(!isClosed())return;deadline.classList.add('closed');deadline.innerHTML='<strong>Step 1 submissions are closed</strong><span>The deadline was 26 September 2026, 11:59 PM IST. New submissions are not accepted.</span>';admissionInput.disabled=true;lookupButton.disabled=true;repositoryInput.disabled=true;submitButton.disabled=true}
async function parseResponse(response){let result;try{result=await response.json()}catch{throw new Error('The submission service returned an invalid response. Please try again.')}if(!response.ok||!result.ok)throw new Error(result.error||'The request could not be completed.');return result}
async function findTeam(){if(isClosed())return showFeedback(CLOSED_MESSAGE);const admissionNumber=admissionInput.value.trim().replace(/\s/g,'').toUpperCase();admissionInput.value=admissionNumber;clearFeedback();clearTeam();if(!admissionNumber)return showFeedback('Enter the team leader’s admission number first.');setBusy(lookupButton,true,'Checking…');try{const r=await fetch(`${API_BASE}/.netlify/functions/stepone-lookup`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({admissionNumber})});showTeam((await parseResponse(r)).team)}catch(e){showFeedback(e instanceof Error?e.message:'The team could not be verified.')}finally{setBusy(lookupButton,false,'Checking…')}}
lookupButton.addEventListener('click',findTeam);admissionInput.addEventListener('input',()=>{clearTeam();clearFeedback()});admissionInput.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();findTeam()}});
form.addEventListener('submit',async e=>{e.preventDefault();if(isClosed())return showFeedback(CLOSED_MESSAGE);if(!verifiedTeam)return showFeedback('First verify the team leader’s admission number.');if(!repositoryInput.value.trim())return showFeedback('Enter the public GitHub repository URL.');clearFeedback();setBusy(submitButton,true,'Submitting…');try{const r=await fetch(`${API_BASE}/.netlify/functions/stepone-submit`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({admissionNumber:verifiedTeam.admissionNumber,repositoryUrl:repositoryInput.value.trim()})});const result=await parseResponse(r);showFeedback(result.message||'Your Step 1 repository has been submitted.','success')}catch(e){showFeedback(e instanceof Error?e.message:'The submission could not be completed.')}finally{setBusy(submitButton,false,'Submitting…')}});
applyDeadlineState();window.setInterval(applyDeadlineState,30000);
